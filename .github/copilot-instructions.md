# Copilot Instructions

## General Guidelines
- Trigger CI publish job only when commit message contains the word "push" (or when creating a tag). Preference for conditional job-level gating in GitHub Actions.